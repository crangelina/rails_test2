<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %>
    <%# render partial: 'microposts/micropost', collection: @feed_items, as: :micropost %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>
